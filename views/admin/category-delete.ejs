<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="container mt-3">
      <div class="row">
        <div class="col-md-12 mt-3 text-center">
          <div class="alert alert-secondary">
            <p>
              Are you sure you want to delete the Category titled "<strong
                ><%= category.categoryName %></strong
              >"?
            </p>
            <form
              method="post"
              action="/admin/category/delete/<%= category.categoryId %>"
            >
              <input
                type="hidden"
                name="categoryId"
                value="<%= category.categoryId %>"
              />
              <button type="submit" class="btn btn-danger">Delete</button>
              <a href="/admin/categories" class="btn btn-secondary">Close</a>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
